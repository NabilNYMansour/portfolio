const Footer = () => {
  return (
    <footer className="mt-auto py-4 text-xs text-muted-foreground z-50 bg-background w-full flex justify-center border-t">
      © {new Date().getFullYear()} Nabil Mansour
    </footer>
  )
}

export default Footer